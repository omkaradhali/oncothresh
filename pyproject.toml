[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oncothresh"
version = "0.1.0"
description = "Clinical threshold evaluation for oncology AI models"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Omkar Adhali", email = "omkar.a2989@gmail.com" }]
requires-python = ">=3.10"
keywords = ["oncology", "pathology", "AI", "evaluation", "clinical threshold"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Healthcare Industry",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.26",
    "scikit-learn>=1.4",
    "pydantic>=2.0",
]

[project.optional-dependencies]
plotting = [
    "matplotlib>=3.8",
]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
]

[project.urls]
Homepage = "https://github.com/omkaradhali/oncothresh"
Repository = "https://github.com/omkaradhali/oncothresh"
Issues = "https://github.com/omkaradhali/oncothresh/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/oncothresh"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--tb=short -q"

[tool.ruff]
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
